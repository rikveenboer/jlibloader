apply plugin: 'java'
apply plugin: 'maven'

group = 'net.rubygrapefruit'
archivesBaseName = 'platform'
version = '0.1'

uploadArchives {
	repositories.mavenDeployer {
		repository(url: uri('.maven'))
	}
}